mixin icon-mono(name)
	svg(class='icon icon-' + name)&attributes(attributes)
		use(xlink:href='icons/sprite-mono.svg#' + name)

mixin icon-multi(name)
	svg(class='icon icon-' + name)&attributes(attributes)
		use(xlink:href='icons/sprite-multi.svg#' + name)